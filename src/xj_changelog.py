# -*- coding: utf-8 -*-
"""内置的更新日志（由 tools/build.py 自动生成，别手改）。

源码见仓库根目录的 CHANGELOG.md。
"""
VERSION = "0.5.4"

TEXT = '# 更新日志\n\n## v0.5.4 (2026-09-14 20:23)\n\n**主题：召唤兽技能克隆 + 界面改版。**\n\n### 🐣 技能克隆\n\n* 召唤兽页 → 技能区新增「**从…克隆**」：从存档里**任意一只**召唤兽把技能整套复制过来\n  —— **别的角色身上的也能当来源**（列表带「角色 / 序 / 名字 / 模板 / 技能数 / 技能名」）；\n* 默认**覆盖**：目标原有技能清空后写成和来源一模一样（「克隆」就该是这个意思）；\n  勾上「合并（保留目标原有技能）」才只补目标没有的，到 12 个为止；\n* 弹窗支持**搜索**（技能名 / 召唤兽名 / 角色名）、双击一行直接克隆；\n* 安全兜底：来源里夹带的**无效技能 id**（`Data\\Skills` 里没有的）会被挡下并报出来，\n  超过 12 个的部分只写满 12 个、剩下的列进提示 —— 不会写出游戏取不到的技能；\n* 语义层 `xj_baby.Babies.clone_skills()` / `all_babies()` / `valid_skill_ids()`，\n  界面只负责挑来源和显示结果。\n\n### 🎨 界面\n\n* 技能栏整体收紧：搜索框 / 技能下拉变窄、「忘掉选中」→「忘掉」，一行放得下不再挤出页签；\n* **窗口标题 / 关于 / 帮助头的版本号改为自动取自 `xj_changelog`**（源头上只有\n  `tools\\build.py` 一处），修复「升了版本标题还挂着 v0.5.3」的漏改。\n\n### ✅ 测试\n\n* 召唤兽回归 **38 → 50 项**（合并 / 覆盖 / 无效 id / 超 12 个 / 跨角色来源 /\n  自己克隆自己报错 / 保存重开还在），其余 13 组回归全部通过。\n\n### 🐞 修复：升级所需经验一直标错了\n\n* **工具以前把 `@limit_exp` 当成「升级所需经验」——那是错的**。\n  游戏脚本 `Game_Actor#gain_exp` 里它是「**累计获得经验**」的计数器，\n  同时也是**体验版的发经验开关**：\n\n  ```ruby\n  @limit_exp ||= 0\n  if @limit_exp > 202123741   # 体验版：累计拿够就不再发经验\n    $tip.say("体验版本, #{name}经验累计获得已达上限：202273024", 1); return\n  ```\n\n  所以乐天凌那一栏显示的是 35000（他累计拿了多少），**不是**升级所需的 332296；\n  没入过队的角色（仙灵儿）压根没这个 ivar → 显示空白。\n* 真正的升级所需经验是**查表**来的（游戏脚本里硬编码的 `$exps`）：\n\n  ```ruby\n  def exp_for_level(lv) = $exps[:actor][lv - 1]\n  def next_level_exp     = exp_for_level(@level + 1)   # 下标就是当前等级\n  ```\n\n  现在按表算：`40 级 → 332296`，跟游戏界面上显示的数一模一样。\n  表已抽成 `src/xj_exps.py`（`tools/gen_exp_table.py` 从游戏脚本生成，换版本重跑即可）。\n* 界面字段名改成**跟游戏里一模一样**（级别 / 获得经验 / 升级经验 / 累计获得经验）：\n  * 「**升级经验**」只读（查表算的，存档里没这个字段，改了也没用），\n    标签用灰色区分，满级时提示里说明；\n  * 「**累计获得经验**」可改，但**超过 202123741 会被拒绝**并在弹窗里说清原因\n    —— 写进去等于把角色改废（游戏判定已达上限，再获得的经验全部作废）；\n    旁边给一个「**清零（解除经验封顶）**」一键按钮；\n  * 「**获得经验**」= `@exp[@class_id]`（本级内经验，升级时减掉门槛重来）；\n    没这个 ivar 的角色读到 `0`，回写时若没有该字段会跳过并提示。\n* 修掉一个潜伏 BUG：`exp()` / `set_exp()` 原来取 `@exp` 这个 Hash 的**第一项**，\n  转过职的存档会读错格子 —— 现在严格按 `@class_id` 定位。\n\n### 🎨 界面：角色页改版（左右布局 + 概览浮窗）\n\n* **「累计获得经验」不再给输入框**——改它没意义（它纯粹是游戏内部的\n  「体验版经验额度」计数器），只在概览里显示当前值 + 是否已封顶；\n* 「已学技能 / 装备 / 五维」那个只读区改名「**属性概览**」，\n  并挪到「基础字段」**右边**，两边等宽、中间可拖（PanedWindow，\n  和「概览 / 快捷修改」页同一套写法）；\n* **满级 / 经验封顶提示改成悬浮浮窗**：鼠标移到「属性概览」上才弹，\n  不再拿一段橙字占版面。浮窗按角色状态给不同内容：\n  * 满级（60）→ 说明「游戏对满级角色直接不发经验，改获得经验看不出变化，\n    要调等级请直接改级别」；\n  * 累计获得经验超线 → 说明「再获得的经验全部作废」并指路清零；\n  * 正常 → 升级还差多少、距封顶还剩多少。\n* 「获得经验」「升级经验」两个输入框也挂了悬浮说明（改这格最容易踩坑）；\n* 顺手修掉浮窗的一个**闪烁 BUG**：`_tip_show()` 内部先调 `_tip_hide()`\n  把 `_tip_key` 清了，而几个 `<Motion>` 处理器却**在调它之前**设 key\n  ——等于每次鼠标动一像素都销毁重建浮窗。现在 key 统一由 `_tip_show(key=)`\n  接收，在清空**之后**写入（数据树 / 防作弊体检 / 背包 / 物品模板四处一起修）。\n\n### 🎨 界面：召唤兽页重排\n\n* **右侧改成两列**：「**常用**」（名字 + 技能）和「**详细信息**」，\n  `columnconfigure(uniform="col")` 锁定**严格等宽**；\n  原来上下堆叠挤到看不见底部信息；\n* 「名字」「技能」原来是带长说明的框标题，现在**简化成纯文字标签**，省一行空间；\n* 技能区的**搜索 / 选择行**和**按钮行**（学会 / 忘掉 / 清空 / 从…克隆）**拆成两行**\n  ——一行塞不下会把按钮挤出可视区；\n* **技能列表去掉「描述」列**（描述在下面展示，不重复占宽度），只留 `技能 id` / `名字`；\n* **技能描述改成只读文本框**（和「详细信息」同款），长描述自动换行、\n  鼠标移上去再弹**浮窗**显示全文，避免描述太长在窗口里看不全；\n  技能列表**逐行悬停**也会显示该行技能的完整描述；\n* 左边「字段 / 当前值」列宽从 `190 / 300` 调成 **`170 / 110`**\n  ——字段列要放得下长标签（「忠诚度（<100 不能参战）」），当前值多是短数字。\n\n### 🎨 界面：角色页标签不再留空隙\n\n* 「基础字段 / 中文属性」的标签原来写死 `width=`（10 / 7 字），\n  撑宽后文字靠左、**右边多出的空白全垫在输入框左边**，看着像空了一大截；\n  现在改成**自适应宽度 + 全角冒号**（照「快捷修改」页的写法），标签紧贴输入框。\n\n### 🐞 修复：改了「获得经验」游戏里没反应\n\n* **根因**：游戏**只在 `gain_exp` 时才动等级**，而 `change_exp` 里根本没有升级逻辑：\n\n  ```ruby\n  def change_exp(exp, show=false)\n    @exp[@class_id] = [exp, 0].max      # 只写值，不升级\n    refresh\n  end\n  ```\n\n  等级只在玩家点游戏里的「升级」按钮时才 +1（`level_up?` 判定 `exp >= next_level_exp`\n  之后调 `actor.level_up`）。而**满级角色 `gain_exp` 第一行就 `return`**，\n  所以对李修远这类 60 级角色，改 `@exp` 在游戏里**永远看不出变化**；\n  另外三个人以前看着"有用"，其实是手动点了升级按钮（`level_up?` 只比大小、\n  不看这是新拿的还是本来就有的经验）。\n* **改法**：要调等级就**直接改「级别」**。新增语义层\n  `xj_game.set_actor_level_full()` —— 改 `@level` 的同时把 `@exp`\n  对齐到**该等级的门槛值**（游戏里 `init_exp` 就是这个语义：\n  `@exp[@class_id] = current_level_exp`），避免出现「60 级但获得经验 0」这种怪档。\n  等级自动夹到 `1..60`。\n* 界面加了勾选框「**改级别时同步「获得经验」**」（默认开），\n  想手工只调经验就把它关掉；输入空串的行不动（不会误清成 0）。\n* 顺手修掉：改完角色后列表**选中行会跳回第一个**（`fill_actors` 无脑\n  `selection_set(第一行)`）—— 改乐天凌却跳到李修远，很容易以为改错了人；\n  现在保持当前选中行并 `see()` 滚动到它。\n* 回归：游戏层 **96 → 123 项**（新增等级夹取 / 经验同步 / 开关关闭不碰 @exp /\n  满级门槛 1091704 等），其余各组回归全部通过。\n\n### 🚨 事故与加固：测试脚本写坏了玩家真档（已恢复）\n\n* **事故**：加界面冒烟测试时，脚本用 `xj_viewer.App(root, save_path=None)`\n  起界面 —— 而 `App.__init__` 会用 `_guess_save()` 找到**玩家真档**并\n  `root.after(200, load)` 排队载入。脚本紧接着手动 `load(副本)`，\n  **200ms 后那个排队回调醒来把 `doc` 换回真档**，之后 `apply_actor()`\n  改的就是真档，一触发保存就写进了玩家的 `save.rvdata2`\n  （乐天凌被改成 60 级 / 87116455 经验）。\n* **恢复**：从 `.huaji2-save-editor\\save.rvdata2.bak.20260914-184910`\n  （写坏前 34 秒的自动备份）恢复，乐天凌回到 40 级 / 35000 / 999999999；\n  被写坏的那份**移走留存**（`_tmp_broken_save.rvdata2`），没有删除。\n* **三层加固**（都不能再犯）：\n  1. **源头**：`App.__init__` 把延迟载入的 `after` id 存进 `_auto_load_job`，\n     新增 `cancel_auto_load()`；`load()` **第一件事就是撤掉排队的自动载入**\n     —— 手动指定了开哪本，就绝不允许被猜出来的那本顶掉；\n  2. **测试脚本**：冒烟脚本一律把**副本路径**传给 `App(save_path=副本)`\n     （`_guess_save()` 完全不跑），并且每次写操作前调 `_guard_real_save()`：\n     `doc.path` 只要指向真档就立刻 `sys.exit(9)`；\n  3. **回归框架**：`tools/run_tests.py` 跑前跑后对真档取\n     `(大小, mtime, sha1)` 指纹，**不一致就判失败并打印前后指纹** ——\n     任何测试再动真档都躲不过。\n* 另外给有同样隐患的一次性探针（`_smoke_exp_gui.py`）挪进 `_tmp/` 归档；\n  冒烟脚本改名 `tools/_smoke_actor_tab.py` 并挂进 `run_tests.py`（新增\n  `actor_tab` 组），没有 tkinter 的解释器会优雅跳过（返回 0）。\n\n\n\n### 🛠 工程\n\n* **GitHub Release 附件名统一为「仓库名 + 版本号」**：`huaji2-save-editor_v0.5.4.exe`\n  （本地 `dist\\画迹2存档工具v0.5.4.exe` 中文名照旧，内容同一个文件）；\n  命名规则写死在 `tools\\build.py` 的 `RELEASE_ASSETS`，发版跑 `tools\\release.py` 即可，\n  不用手敲名字；\n* 新增 `.gitattributes`：文本统一按 LF 存 —— 之前工作区被写成 CRLF 后，\n  `core.autocrlf=false` 会让每个 `.py` 都变成「整文件重写」的假 diff。\n\n## v0.5.3 (2026-09-14)\n\n**主题：修复 v0.5.2 的坏档 BUG —— 祈福池改成超大数字后，存档在读档时崩\n`NoMethodError: undefined method \'condition\' for RPG::MoveRoute`。**\n\n### 🐞 根因（真凶：对象编号错位）\n\n* Ruby Marshal 里**每个写出的大整数（`l` 类型）都要占一个对象编号**；\n  v0.5.2 把祈福池改成 9999999999（超出 Fixnum 范围）时，工具写出了大整数\n  **却没给它编号**，而游戏按 Ruby 规则给每个大整数编号 ——\n  从那以后全档的 `@N` 对象链接**整体错位 4 个**，事件页 `@pages` 链到了\n  `RPG::MoveRoute` 上，读档直接 `undefined method \'condition\'`；\n* 错位一旦写进字节就**固化**了（文件自身是自洽的，只是描述了一张错位的图），\n  坏档无法就地修复，只能从健康备份恢复。\n\n### 🛠 修复（三层防护）\n\n* **序列化层**（`xj_marshal.py`）：整数按**写出的形态**决定占不占编号 ——\n  装得下 Fixnum 写 `i`（不占），装不下写 `l` 大整数（占），和 Ruby 完全一致；\n* **改值层**（`xj_model.py`）：整数修改**跨过 Fixnum 边界**（±42.9 亿）时，\n  自动升级为**整档重写**（就地补丁无法同步全档 `@N` 索引）；\n* **保存守卫**（`xj_model.py`）：每次保存前**重解析 → 重序列化**逐字节自检，\n  对不上就拒绝写盘 —— 以后再有什么编号错位的坏档，**拦在写盘之前**，\n  原文件一个字节都不会动。\n\n### 🩹 坏档救援\n\n* 受影响的存档（2026-09-14 08:31 之后用 v0.5.2 保存过的）已从最后一份\n  健康备份恢复，并重新套用全部修改（金钱 / 步数 / 次数 / 祈福池）；\n* 备份目录（`.huaji2-save-editor`）里 08:31 之后的备份也是坏的，\n  **不要用「恢复最新」直接回滚到那些点**。\n\n### ✅ 测试\n\n* 新增复现脚本证明：修复后把祈福池改成 9999999999，事件页普查 0 坏、\n  重解析逐字节自洽；\n* 全套回归 281 项通过：roundtrip 18 / 存档层 14 / 游戏层 87 / 召唤兽 38 /\n  语义等价 39 / 备份 29 / 数据表 56。\n\n## v0.5.2 (2026-09-14)\n\n**主题：祈福池修改 + 概览页左右分栏 + 备份文件归档。**\n\n### 🏮 祈福池\n\n* 新增**祈福池储备修改**：角色气血 / 角色魔法 / 宠物气血 / 宠物魔法\n  （存在 `$game_party` 的 `*_pool` 四个键里）；\n* 这 4 项**不在游戏防作弊检查范围**，直接写即可，负数自动钳 0；\n* 和金钱 / 步数 / 存档次数 / 战斗次数**共用同一个「应用」按钮**，\n  打开存档自动回填当前值。\n\n### 🎨 概览页布局重排\n\n* 概览页改成**左右分栏**（PanedWindow，各 50%，中间可拖）：\n  左 = 快捷修改，右 = 防作弊体检；\n* 快捷修改 8 个输入框**单行 4 列平均分配**（金钱/步数/存档次数/战斗次数一行，\n  祈福池 4 项一行），标签按文字自然宽度紧贴输入框；\n* 存档概况收进左栏快捷修改下方；体检表格的「说明」列改成\n  **鼠标悬浮条目弹出浮窗**，表格更宽松；\n* 移除 v0.5.1 加的 MAP_SCROLL / PLOTING 开关勾选框（实测没有实际作用）。\n\n### 🗄️ 备份归档（修复）\n\n* 写回存档时自动生成的 `save.rvdata2.bak.<时间戳>` 安全备份\n  **不再散落在游戏根目录**，统一收进 `.huaji2-save-editor\\` 备份目录\n  （和自动备份、手动备份放一起，存档管理页统一可见）；\n* 已散落在游戏目录里的旧 `.bak.` 文件也一并归位。\n\n## v0.5.1 (2026-09-13)\n\n**主题：防作弊补全家底 —— 改完金钱再玩也不会被判作弊。**\n\n### 🛡️ 防作弊（核心修复）\n\n* **找到"玩一会儿还是被判作弊"的真凶**：游戏除了 Lock 校验和，还在\n  `$game_system.security` 里记了**五类账**（金钱 / 物品 / 变量 / 人气 / 贡献），\n  每次数值变动都拿账和实际值比对，对不上立刻往 `@keyword` 写 `NE!` 判作弊。\n  旧版工具改金钱只重算了 Lock 的 `@master`，**从没同步过这笔金钱账**\n  （实测真实存档里金钱账是 -9.7 亿、金钱 2994 万，`@keyword` 里已经一串 `NE!`）；\n* 概览页「校验码修复」按钮改名 **「防作弊检测并修复」**，一次查完全部检查点\n  并修好：Lock 校验和、周期超限、五类记账、`@cheated`、`@keyword`、机器码；\n* 体检表格新增：Lock 校验和行、**金钱记账**行、变量/人气/贡献记账行、\n  `@keyword` 作弊记录行（VNE / NE! / 修改器关键字一眼看全）；\n* 清除作弊标记时把 **`@keyword` 整个清空**（以前只去掉 `VNE`，`NE!` 还留着）。\n\n### 💰 金钱\n\n* 界面文案统一叫**「金钱」**（背包栏、概览、数据树注释、帮助页全部同步）；\n* 金钱填超过上限 30,000,000 时，**自动改成 25,000,000（上限的 5/6）**并弹窗\n  说明 —— 不再贴着上限留隐患；\n* 改金钱时 Lock 校验和**和**游戏金钱账一起同步，游戏里花钱赚钱都不会触发 `NE!`。\n\n### 🎒 背包\n\n* 背包格子和物品模板列表支持**鼠标悬浮看物品完整说明**\n  （取自 Data 表的 `@description`，武器/防具/道具按对象类自动选表）；\n* 召唤兽字段表里那个"说明"列其实什么都没有，**砍掉**，界面更干净。\n\n### 🎮 快捷修改 / 开关\n\n* 原来的「开关 / 变量」页签**从 Tab 栏里拿掉**（本作 switches 只有 3 个，\n  variables 总共 30 个但没匹配到"遁铺之术 / 祈福池"这类任务开关，\n  贸然加反而有副作用）；\n* 2 个有用的 switch（MAP_SCROLL、PLOTING）挪到**快捷修改**里做两个勾选框；\n* 概览页原来挤在金钱输入框下面的**两行灰色长说明被体检表盖住了** ——\n  全改成**鼠标悬浮说明 Label 就能看到的浮窗 tooltip**，不再挡输入框；\n* 自动备份文件名里的备注清空（打开/保存前自动备份现在只留时间戳）。\n\n### 🎯 等级模拟（和游戏行为对齐）\n\n* 改角色 / 召唤兽**等级时自动调整潜能 & 五维**：\n  - 升级（差 +N）→ 潜能 +5×N，五维每个 +1×N（和游戏 `level_up` 一致）；\n  - 降级（差 -N）→ **洗点**（五维 + 潜能全清零，等价于游戏里的「超级金柳露」）。\n* 走 GameEditor 统一入口，**maxlv 预设按钮**、角色/召唤兽页"应用"都会触发。\n\n### 🗂️ 全部解析数据\n\n* 节点**"说明"列从 Treeview 里砍掉**，改成**鼠标悬浮节点时弹浮窗 tooltip**，\n  同时把「值」列从 400px 加宽到 600px 能多看点内容。\n\n### 🐛 顺带修复\n\n* 角色的 `@活力` 是浮点字段（如 `200.0`），打开角色页直接点「应用」会报\n  "要填数字的地方填了别的东西"——数字输入框现在能吃小数，写回时保留字段\n  原本的浮点类型。\n\n### ✅ 测试\n\n* 游戏层回归 60+ → **86 项**：金钱账同步、负数记账读写、`resync_all_security`\n  五类账、金钱 5/6 安全钳制、`@keyword` 清空；\n* GUI 快速回归 111 → **115 项**：界面改金钱同步记账、超限自动压到 2500 万等；\n* 页签从 11 个缩到 10 个（开关 / 变量页已隐藏，有用的 switch 挪到快捷修改）。\n\n---\n\n## v0.5.0 — 2026-09-13\n\n**主题：召唤兽界面更好用 + 存档管理再优化。**（本版只改源码/界面，打包没跟着走）\n\n### 🐣 召唤兽\n\n* 召唤兽列表和技能列表都补了滚动条，**最下面一行的内容不再被卡掉**；\n* **搜索技能后，下方描述立刻跟着变**（不用再点一下）；\n* 技能下拉框按 **↑ / ↓** 会**直接切换技能并刷新描述**，不用先打开弹层、\n  再回车确认；\n* 删掉「改基础名」按钮 —— 基础名 `@name` 管立绘/音效（游戏取\n  `read_note(\'battler\') or name`），改坏了会取不到立绘/音效，不该改；\n* 「改显示名」不再查名字表、也不再弹确认框，显示名只是给人看的，随便改。\n\n### 💾 存档管理\n\n* 「立即备份」成功后**直接弹窗填备注**（可留空＝不写备注）；\n* 新增按钮**「删除无备注」**：一键清掉所有没写备注的备份，带备注的保留；\n* 按钮重新排序：立即备份 → 编辑备注 → 恢复选中 → 恢复最新 → 删除选中 →\n  删除无备注 → 删除非最新 → 刷新 → 打开备份目录。\n\n### ✅ 测试\n\n* `tests/test_gui_quick.py` 107 → **111 项**（技能描述即时刷新、↑/↓ 切技能、\n  「立即备份」填备注、「删除无备注」只删没备注的）。\n\n---\n\n## v0.4.9 — 2026-09-13\n\n**主题：存档管理更贴心 + 界面优化 + 召唤兽潜能修对。**\n\n### 💾 存档管理（「删除非最新」不再误伤手动档）\n\n* 「删除非最新」只清**自动备份**：留最新的一份 + **所有手动备份**；\n  备份全是手动档时直接提示不删 —— 手动档是你主动留的，不该被自动清理带走；\n* 列表新增**序号**列；每份备份可以点「编辑备注」写说明（存在备份旁的 `.txt`，\n  留空就是删备注），多台电脑之间拷贝存档目录也能带着备注走；\n* 配套把 `empty_slots()` 修对：被「清空」的格子（置 nil）现在能重新放东西，\n  之前会被误当成“已占用”，导致清掉的格子加不了物品/套不了模板。\n\n### 🖥️ 界面\n\n* 概览页信息框、存档列表、角色/背包/召唤兽/技能/开关变量/机器码/数据表等\n  窗口都补上了滚动条，内容多了也能滚到；\n* 概览页「快捷修改」改成 2×2 排布，更紧凑。\n\n### 🐣 召唤兽潜能\n\n* 新增召唤兽的**初始潜能**修对了：之前误用了主人等级，现在按游戏\n  `Game_Baby_Attr` 的规则用**召唤兽自己**的等级（模板 `@initial_level`），\n  1 级召唤兽潜能 = 5，跟游戏里一模一样。\n\n---\n\n## v0.4.8 — 2026-09-13\n\n**主题：把“清作弊标记”彻底修对 —— 上一版写进去的是整数 `0`，而 Ruby 里 `0` 是“真值”，\n所以游戏照样判你作弊（读档一段时间后弹「存档异常」退出）。**\n\n### 🐞 为什么上一版“清了标记还是被惩罚”\n\n游戏判断作弊就一句：`if $game_system and $game_system.cheated`。\nRuby 只有 `false` / `nil` 是假值 —— **整数 `0` 是真值**。\n\n上一版把 `@cheated` 写成 `false` 时，落盘的实际字节是 Marshal 的整数 `0`\n（`i\\x00`），游戏读回来是 `0` → 条件成立 → 解档后照样：\n`v = Graphics.frame_count - 0`，`v > 60*60*25-123` 就 `msgbox "存档异常！"` 然后 `exit`。\n\n修法（都在序列化层，顺手也修好了开关/其它布尔字段）：\n\n* `src/xj_edit.py` / `src/xj_marshal.py`：**节点里存的是 Python `bool` 时就写 `T`/`F`**，\n  绝不写成整数 `0`/`1`；\n* `xj_game.is_ruby_false()`：判断 `@cheated` 只看“是不是 `false`/`nil`”，\n  整数 `0` 也算**有问题**（老版本写入的 `i0` 能被体检认出来并修好）。\n\n### 🧹 新增：一次清掉**所有**存档（含 AutoSave）\n\n`AutoSave\\save00..09` 当时也带着 `@cheated` —— **读自动存档一样会被惩罚**，\n只修主存档是不够的。\n\n* 概览页「防作弊体检」多了一个按钮：**清理所有存档（含 AutoSave）** ——\n  扫 `save.rvdata2` + 游戏根下其它 `save*.rvdata2` + `AutoSave\\*.rvdata2`，\n  挨个按规则修复 + 清作弊标记 + 同步物品计数，每个都先备份到 `.huaji2-save-editor`；\n* **载入存档时如果发现 `@cheated` 不是 `false`**，会立刻弹窗说清楚（20 分钟转圈、\n  25 分钟弹「存档异常」、战斗里崩 `disposed sprite`）并问要不要顺手修好；\n* 概览页「防作弊体检」下方多了一行红字状态：正常就是 `✔ 体检 N 项全部正常`，\n  有问题就写明是哪几项。\n\n### 🔤 名词澄清：两种“校验”不是一回事\n\n游戏里有**两套**机制，上一版界面容易混：\n\n| 名称 | 管什么 | 怎么修 |\n| --- | --- | --- |\n| **校验码修复（Lock 校验和）** | 存银 / 步数等字段旁边那个 `Lock`（`v*91+45+seed/800`） | 「校验码修复（Lock 校验和）」按钮 |\n| **作弊标记 `@cheated`** | 游戏自己 300 帧查一遍超限、物品计数校验 | 「清除作弊标记」/「清理所有存档（含 AutoSave）」/ `tools\\clear_cheat.py --all` |\n\n所以“防作弊校验也没用”是点错了按钮 —— 它修的是 Lock 校验和，跟 `@cheated` 无关。\n\n### ✅ 测试\n\n* 新增 `tests/test_save_files.py`（**24 项**）：`save_files()` 能扫到 AutoSave、\n  `--dry-run` 不写盘、修完 `@cheated` 是 **Marshal 的 `F`**（不是 `i0`）、\n  手工塞一个老版本的 `i0` 能被认出来并修回 `false`、备份落在 `.huaji2-save-editor`；\n* `tests/test_gui_quick.py` 103 → **107 项**（体检能报出作弊标记、载入即提醒并顺手清掉）。\n\n### 🧰 命令行\n\n```\npython tools\\clear_cheat.py --all --dry-run   # 只看哪些存档有问题\npython tools\\clear_cheat.py --all             # 全部修一遍（各自先备份）\n```\n\n## v0.4.7 — 2026-09-13\n\n**主题：把“进战斗就崩（`disposed sprite`）”的真相说清楚 —— 那是游戏的作弊惩罚，\n不是数据坏了；顺手修 4 处体验问题。**\n\n### 💥 `Script \'0000\' line 29491: RGSSError occurred. disposed sprite`\n\n脚本 29485-29495 行长这样：\n\n```ruby\nif $game_system and $game_system.cheated\n  v = Graphics.frame_count - $game_system.cheated\n  if v > 60*60*25-123\n    msgbox "存档异常！#{GET_HARD_DISK_CHARACTER.call}"; exit\n  elsif v > 60*60*20-123 and !$timer.has?(\'cheating_circle\')\n    $timer.every(2, proc{|i| ... $game_player.sprite.zoom_x = rand(0.8..1.0); ... })\n    $timer.every(300, proc{|i| s = $game_player.sprite; ... unless s.disposed? })\n  end\nend\n```\n\n`@cheated` 一旦被记下（周期检查发现超限，或物品计数校验对不上），20 分钟后\n开始“惩罚”（画面转圈/缩放）；而它去碰的 `$game_player.sprite` 在**战斗中已经被\ndispose** → 就是你看到的 `disposed sprite`。\n\n**工具已加保存前体检**：\n\n* Ctrl+S 前若还有超限项/作弊标记，会弹窗说清楚并问要不要**顺手修好**\n  （数值按规则修复 + 清作弊标记 + 同步物品计数校验），再保存；\n* 也可以自己在「概览」页点：**体检 → 一键按规则修复 → 清除作弊标记 → 同步物品计数校验**。\n\n⚠ 已经进过惩罚状态的存档，清掉标记后**要在游戏里重新读一下档**（惩罚计时器在\n内存里，不跟着存档走）。\n\n### 🪟 另外 4 处\n\n* **新增召唤兽窗口居中**（以前会跑到屏幕左上角）；\n* 新增召唤兽窗口去掉了「只看小孩 181-187」勾选和那一排小孩按钮\n  （小孩本来就在列表里，按池搜索即可）；\n* 修好「加这只」按钮（上一版被误改坏，点了会 NameError）；\n* **召唤兽技能列表加了「描述」列**，下面还有一行“技能 #id 名字：描述”\n  （选下拉、点技能行都会更新）；\n* **改字段「应用」后不再跳回第一行**（保持原来选中的那个字段）。\n\n### ✅ 测试\n\n* `tests/test_gui_quick.py` 99 → **103 项**（技能描述列、应用后不跳行）。\n\n### 🧰 顺手加的命令行工具\n\n* `tools\\clear_cheat.py`：给存档“清作弊标记 + 同步物品计数校验”，等价于界面那两个按钮；\n  默认会先往 `.huaji2-save-editor` 备份一份，可加 `--dry-run` 只看不改。\n\n## v0.4.6 — 2026-09-13\n\n**主题：召唤兽功能补全（照画迹1 的编辑器）＋「新增召唤兽」——小孩终于能自己加了。**\n\n### 🥚 先说结论：小孩没有任何道具能开出来\n\n把游戏脚本翻了一遍：`#181 小精灵`、`#182 小毛头`、`#183 小魔头`、`#184 小仙灵`、\n`#185 小仙女`、`#186 小丫丫`（连 `#187 善财童子`）在 `Data\\Actors` 里的备注都是\n`data = :神兽资质3`，而**所有开蛋道具**只抽这些：\n\n| 道具 | 抽出范围 |\n| --- | --- |\n| 初级孵化蛋 #110 | `rand(21..23, 25..63)` |\n| 中级孵化蛋 #111 | `rand(64..95, 127..134)` |\n| 高级孵化蛋 #112 | `rand(96..126)` |\n| 神兽孵化蛋 #113 | 备注池 `:神兽资质` ∪ `:神兽资质2` |\n| 神兽蛋 #221 / #222 | `:神兽资质` / `:神兽资质2` |\n\n`:神兽资质3` 这个池子在脚本里**只有一处引用**（26253 行的资质配置表），\n没有任何道具/公共事件/商店会用它；敌人备注里的 116 条捉宠表也没有\n`baby_id = 181~186`。全脚本唯一直接发小孩的是调试方法 `setup_battle_test_babys`\n（第 8645 行 `(181..187).each{|i| a.add_baby(i) }`），正常玩触发不到。\n**所以：只能靠改存档塞进去 —— 现在工具里一键加。**\n\n### 🐣 新增召唤兽（`src/xj_baby.py`，规则照抄 `Game_Baby.new`）\n\n选角色 → 点「新增召唤兽」→ 列表里挑一只（可按名字/id/池搜索、只看神兽、\n只看小孩 181-187、可勾“变异”）→「加这只」。造出来的和游戏本体一模一样：\n\n* 名字 / 立绘 / 职业 / 等级 取 `Data\\Actors` 模板（等级 = 模板 `@initial_level`）；\n* **资质**：普通 = 上限 − `rand(201/401/301/201/201)`（变异时区间 ×0.66）；\n  神兽 = **上限定值**；\n* **成长 / 寿命**：普通 = 上限 − `rand(6)/100`、上限 − `rand(13)×100`；\n  神兽 = 定值，寿命 = `:infinite`（永生）；\n* **五维**：普通 = `10 + 主人等级 + rand(11)`；神兽 = `20 + 主人等级`；潜能 = 主人等级 ×5；\n* 忠诚 100、五行（金木水火土）随机、`@dyeing`（神兽 0 / 普通 1）、\n  `@signature` / `@seed` / `@seeds` 随机；\n* **技能**：神兽 = 该职业全部技能，普通 = 每条 40% 概率（上限 12 个，游戏规则）；\n* **气血/魔法按公式算满**：`real_mhp = 体质×成长×6 + 体力资质×主人等级÷1000`，\n  `real_mmp`/`real_atk`/`real_def`/`real_agi` 同理（见 `xj_baby.py` 文档串）；\n* `@master` 指回主人、`@attr.@master` 指回自己（自引用会正确写成 `@N`）。\n\n小孩的配置（`神兽资质3`）：**攻 2400 / 防 2400 / 体 7500 / 法 4800 / 速 2100 /\n躲 2100、成长 1.8、永生** —— 全游戏最高档。\n\n`$baby` 资质表抽成了 `src/xj_baby_data.py`（114 种普通召唤兽 + 3 个神兽池，\n由 `tools/gen_baby_data.py` 从脚本自动生成，和 `xj_changelog.py` 一个套路）。\n\n### 🐾 召唤兽页补全（参考画迹1）\n\n* 上面多了**一览表**：序 / 名字 / 模板 / 等级 / 成长 / 忠诚 / 寿命 / 六项资质 /\n  技能数 / ★出战；\n* 按钮：**新增召唤兽**、**设为出战**、**放生（删除）**、**恢复模板名**；\n* **名字**：改显示名（`@attr.@name`）/ 改基础名（`@name`），旁边有\n  “名字在不在游戏名字表里”的提醒（不在表里战斗可能取不到立绘/音效），\n  还有「恢复模板名」一键还原；\n* **技能**：搜索 + 模板下拉 + 「学会」/「忘掉选中」/「清空」，上限 12 个；\n* 预设补全：**六项资质+500**、**成长+0.1**（原有的 +100 / 满级 / 忠诚满 /\n  寿命满 / 五维+10 保留）；\n* 神兽的「寿命 = 永生」现在显示成“永生”，也能直接改成数字\n  （顺手修了 `get_int` 遇到符号会炸、以及符号字段改不了的问题）。\n\n### ✅ 测试\n\n* 新增 `tools/test_baby.py` **37 项**：数据表/池、新增神兽与普通（含变异）、\n  资质定值、满血计算、技能全学、存盘重开、技能增删、名字表校验、出战、放生；\n* `tests/test_gui_quick.py` 86 → **99 项**（召唤兽新界面全流程：加小孩 →\n  改技能 → 改名 → 恢复 → 出战 → 放生 → 开新增窗口）；\n* 顺带修了 GUI 用例的一个环境问题：背包第 1 页满了时，空格在别的翻页上，\n  用例现在会先切到那一页再选格子。\n\n## v0.4.5 — 2026-09-13\n\n**主题：存档管理按你的要求改（目录名 / 恢复最新 / 删除非最新 / 不再多留备份）。**\n\n* 备份目录名改成 **`.huaji2-save-editor`**（名字带点，资源管理器里默认隐藏）。\n  老版本用过的 `huxji2-save-editor` / `huaji2-save-editor` 里的备份**照样列出来、\n  照样能恢复/删除**，只是新备份不再往里写。\n* 「恢复上一个」→ 改名 **「恢复最新」**：把存档换回**上一次修改之前**的状态\n  （＝用最新的一份备份覆盖当前存档）。\n* 新增 **「删除非最新」**：只留最新的一份备份，其余全删（清理用的）。\n* 「恢复选中」/「恢复最新」**不再额外留“恢复前”那份备份**（要留手动点「立即备份」）。\n* 保存前仍会自动留一份 `auto` 备份（同一份文件 90 秒内不重复）。\n* 按钮：立即备份 / 恢复选中 / **恢复最新** / 删除选中 / **删除非最新** / 刷新 / 打开备份目录\n\n## v0.4.4 — 2026-09-13\n\n**主题：孵化蛋真的能用了（找到“键类型”这个真凶）＋ 新增「存档管理」页。**\n\n### 🥚 为什么重抽了内容还是报错（真凶：`@attr` 的键写成了符号）\n\n用 `tools\\probe_attr_node.py` 把两个蛋逐层摊开，终于看清了：\n\n```\n游戏给的蛋（槽0）：@attr = { "data" => { :type => :baby_egg, :data => {:id=>57} } }\n                        ↑ **字符串**键（StrNode）\n工具重抽的（槽1）：@attr = { :data   => { :type => :baby_egg, :data => {:id=>156} } }\n                        ↑ 符号键（SymbolNode）——游戏读不到！\n```\n\n而游戏使用孵化蛋时执行的是（公共事件 24）：\n\n```ruby\n$game_player.actor.add_baby($item_obj.data[:data][:id])\n```\n\n`item.data` 读的是 `@attr["data"]`（字符串键）—— 工具写成符号键，\n游戏那边 `item.data` 就是 nil → `nil[:data]` →\n**`NoMethodError: undefined method \'[]\' for nil:NilClass`**（就是你看到的那句）。\n\n修了三处：\n\n1. **写**：`@attr` 现在与游戏完全一致，外层用字符串键 `"data"`（内层仍是符号，\n   和游戏一样）；\n2. **读**：`xj_save.hash_get()` 以前不会解引用键（`I"data"{E=true}` 这种包装读不到）、\n   也不认字节串键 —— 所以“游戏给的蛋”工具读不出来。【两个都修了】\n3. **体检/修复**：新增检查“**键类型不对**（老版本工具写成符号）”，\n   「一键修复」会把它重写成字符串键，**内容一个不动**。\n\n> 实测（存档副本）：修复前 槽1 键=SymbolNode（游戏读不到）、槽0 键=StrNode；\n> 「一键修复」后 槽1 → 键=StrNode、`{id=>156}` 保留，槽17（空内容）并入槽0，\n> 保存重开依旧。槽0（游戏给的蛋）现在工具也能读出 `蛋→黑山老妖(57)` 了。\n\n### 💾 新增「存档管理」页\n\n规范化存放：备份统一放在**存档旁边**的子目录里（v0.4.4 时叫 `huxji2-save-editor`，\n**v0.4.5 起改为 `.huaji2-save-editor`**，并能读老目录），\n归档名 `save.2026-09-13_014530.rvdata2`。\n\n| 按钮 | 作用 |\n| --- | --- |\n| **立即备份** | 把当前存档复制一份进备份目录 |\n| **删除选中** | 删掉选中的备份（支持多选） |\n| **恢复选中** | 用备份覆盖当前存档，并自动重新载入；\n                **动手前会自动先存一份“恢复前”** |\n| **恢复上一个**（v0.4.5 起改名「恢复最新」） | 退回上一次「恢复」之前（撤销），点错了也能救 |\n| 刷新 / 打开备份目录 | 列表刷新、在资源管理器里打开 |\n\n备份类型共三种：`手动` / `自动`（保存前自动留一份，同一份文件 90 秒内不重复）/\n`恢复前`。列表显示时间、类型、大小、文件名、备注，双击一行即恢复。\n核心逻辑在 `src/xj_backup.py`，只做文件复制/删除，**不解析存档** ——\n所以档案坏了也能用它救回来。\n\n### ✅ 测试\n\n* 新增 `tools\\test_backup.py` 18 项（命名/列表/备份/恢复/撤销/去重/删除/目录沿用）\n* `tools\\test_game_layer.py` 69 项（+ 字符串键写入、按游戏的方式读、\n  符号键体检与修复、内容不丢）\n* `tests\\test_gui_quick.py` 85 项（+ 存档管理页四个按钮、故意改坏再恢复再撤销）\n* `tests\\test_gui.py` 26 项（页签 11 个）\n* 新增 `tools\\runlog.py`：把命令输出自己写成 UTF-8（避开 PS 重定向的 UTF-16 坑）\n\n## v0.4.3 — 2026-09-13\n\n**主题：报错提示全中文 ＋ 背包能直接看到/重抽“运行时内容” ＋ 更新日志写进 exe ＋ 机器码单独一页。**\n\n### 🈶 报错提示不再冒英文\n\n以前某个操作抛异常时，弹框和状态栏会直接显示 Python 的英文消息\n（`NoMethodError`、`object is not subscriptable`、`Access is denied` …），\n看完又变回中文。现在两层翻译：\n\n* `zh_error()`：按异常类型给中文说法（找不到对应的东西 / 文件被占用 /\n  存档结构不对…），原文附在“技术细节”里；完整堆栈照旧写 `error.log`；\n* `zh_text()`：字符串层面把常见英文短语换成中文（给那些直接显示 `str(e)` 的地方）。\n* 宿主 `XJCodec32.exe` 里两句英文提示也改成了中文。\n\n### 🎒 背包：“内容”列 + 重抽\n\n* 格子列表新增 **“内容”列**，直接写出运行时内容摘要：\n  `蛋→狐狸精(37)`、`技能书→高级必杀(113)`、`阵型→龙飞阵` ……一眼就能看出\n  哪颗蛋缺“【诞生对象】”；\n* 新增 **「给选中的格子重抽内容」** 按钮：按游戏脚本的规则重新生成一份\n  （可以在“孵出/开出对象 id”里指定孵出哪只，留空就随机抽）。\n* `GameEditor.set_payload()` / `payload_summary()`（新增 API）。\n\n> 实测：`tools/probe_payload_now.py --fix` 在存档副本上跑「一键修复」，\n> 初级孵化蛋 → `{id:37}`（诞生物：狐狸精）、神兽孵化蛋 → `{id:150}`（超级神牛）、\n> 魔兽要诀 → `skill_code 113`，保存后重开依旧带内容。\n\n### 📖 exe 的“更新日志”写死了\n\n以前是去读运行目录的 `CHANGELOG.md` —— onefile 的 exe 运行在临时解包目录里，\n读不到就成了空白。现在 `tools\\build.py` 会把 `CHANGELOG.md` 生成成\n`src\\xj_changelog.py`（内置文本）编进 exe，同时仍然把 `CHANGELOG.md`\n放进 `dist\\`（想改文案不重编译也能看）。\n\n### 🖥 机器码独立一页（照画迹1）\n\n“概览”页底部只留一行概述，完整功能搬到新的 **「机器码」页**：\n存档记录列表、本机机器码、输入框 + 按钮：\n读取本机机器码 / 读存档里第一个 / 加入存档（追加，推荐）/ 替换成这个 /\n用本机机器码替换 / 清空存档记录，右下角还有**操作记录**。\n\n### ✅ 测试\n\n* `tools\\test_game_layer.py` 66 项（+ 内容摘要、重抽/指定、对空格子重抽报错）\n* `tests\\test_gui_quick.py` 77 项（+ 机器码页与四个按钮、内容列、重抽内容）\n* `tests\\test_gui.py` 25 项（页签变 10 个）\n\n## v0.4.2 — 2026-09-13\n\n**主题：修「工具加进去的孵化蛋一用就报错」＋ 背包里装备不显示名字。**\n\n### 🥚 孵化蛋为什么会报错（根因）\n\n游戏里这类东西的“内容”是**发给你的时候现抽的**，存在存档物件的 `@attr`（脚本里叫\n`item.data`）：\n\n```ruby\ndef 孵化蛋(item)\n  i = item.id - 110\n  item.data = { type: :baby_egg, data: { id: [ rand(21..23, 25..63), ... ][i] } }\nend\n```\n\n以前工具是照 `Data\\Items.rvdata2` 的模板“凭空造”，`@attr` 只能是空 Hash，\n游戏一用就 `item.data[:data][:id]` → **`undefined method \'[]\' for nil:NilClass`**\n（就是你看到的那张报错图）。\n\n现在做对了三件事：\n\n1. **优先克隆存档里已有的同款**（连运行时内容一起拄）——最保险；\n2. 存档里没有同款时，**按游戏脚本里的生成规则现造一份**\n   （新增 `src\\xj_payload.py`，把脚本 `Game_Party#孵化蛋/神兽蛋/鬼谷子/进阶石/\n   制造指南书/百炼精铁/魔兽要诀/上古锻造图策/天眼珠/人参果/真知棒/石头/元宵/\n   导航旗 ` 的规则照抄成 Python）；\n3. 背包页新增「**孵出/开出对象 id**」输入框（留空＝按游戏规则随机），\n   写入时也会在提示行提醒“这件东西属于运行时内容”。\n4. **已经加坏的能修**：「背包体检」会把 `@attr` 是空的这类格子列出来，\n   「一键修复」直接补上内容（实测你的存档里有 4 件：初级孵化蛋×2、\n   神兽孵化蛋×99、魔兽要诀×2）。\n\n### 🎽 背包里的装备不显示名字\n\n背包（`@items`）里是**道具/武器/防具混装**的（召唤兽装备也是武器/防具），\n以前统一拿 `Items.rvdata2` 查名字，查不到就成 `?`。现在按**物件自己的类**选表：\n`RPG::Weapon` → Weapons、`RPG::Armor` → Armors，再退到对象自带的 `@name`。\n实测：槽 13 `RPG::Weapon id=183` → 铁骨扇、槽 15 `RPG::Armor id=192` → 竹编护腕。\n\n### ✅ 测试\n\n* `tools\\test_game_layer.py` 59 项（新增：识别运行时内容物品、自动/指定生成的孵化\n  蛋内容、克隆同款、坏蛋体检 + 一键修复、混装武器装备名字）\n* 其余组不变，一键全绿：`python tools\\run_tests.py`\n\n## v0.4.1 — 2026-09-13\n\n**主题：修「重新载入会闪一下 XJCodec32.exe 黑框」＋ 背包功能照画迹1 补齐 ＋ 机器码。**\n\n### 🪟 不再闪黑框\n\n打包成 exe 后，每次加解密都要起一次 32 位宿主，Windows 会给它弹一个控制台窗口\n（`--windowed` 的主程序 + 控制台子进程），所以「重新载入」时闪一下。\n画迹1 的编辑器也是这么解决的：**`CREATE_NO_WINDOW` + `STARTF_USESHOWWINDOW`**，\n现在 `xj_codec._run()` 一律静默启动宿主。\n\n### 🎒 背包（照画迹1 的编辑器的功能补齐）\n\n画迹1 的背包页是「左边格子列表 + 右边可搜索的物品模板 + 写入/清空/异常修复」，\n现在画迹2 也齐了：\n\n| 功能 | 说明 |\n| --- | --- |\n| **物品模板列表 + 搜索** | 右边列出 `Data\\Items.rvdata2`（武器/防具同理）的 300 件物品，按名字/说明/id 过滤 |\n| **写入选中的格子** | 双击模板（或点按钮）＝把那一格**换成**这件物品；原有数量会保留 |\n| **放进第一个空格子** | 不用先挑格子 |\n| **选中格子自动回填** | 点格子会把 id / 数量填进输入框（画迹1 的 `load_pack_edit`） |\n| **本页全部 99** | 批量把当前页已有格子的数量设成 99 |\n| **背包体检** | 结构坏 / 物品 id 在 Data 表里不存在 / 数量 0 / 超单格上限 / **同一件东西占两格** |\n| **一键修复** | 重复格合并数量、超上限截断到 99、坏格子清空，并同步物品计数校验 |\n\n顺带真抓到一个问题：这份存档里 **9 号格（佛手×1）和 0 号格（佛手×13）重复**，\n游戏按 id 取数量时会算不清 —— 点「一键修复」就会合并成 14 个。\n\n### 🖥 机器码（存档绑定）\n\n游戏用 `main.dll!get_hard_disk_character()` 取本机机器码，存在\n`$game_system.config[:hard_disk_code]`（**数组**），启动时 `include?` 比对，\n不在里面就弹「存档异常」—— 所以存档拷到别的电脑会打不开。\n\n* 宿主新增 `machine` 模式：调 `get_hard_disk_character` 取本机机器码\n  （实测这就是存档里那个值：`630693299`）；\n* 界面「概览 / 快捷修改」新增**机器码面板**：显示本机机器码 / 存档记录的机器码、\n  是否匹配，按钮「读取本机机器码」「用本机机器码填上」\n  **「加入存档（追加，推荐）」**「直接替换成这个（只留一个）」；\n* 「防作弊体检」多一项「机器码」，不匹配会标红；\n  「一键按规则修复」也会自动把本机机器码**追加**进存档；\n* `docs\\存档格式.md` 补了机器码那一节。\n\n### ✅ 测试\n\n* `tools\\test_game_layer.py` 52 项（新增：机器码读/追加/替换/恢复、模板表与搜索、\n  `set_item` 换物品、批量改数量、背包体检+修复）\n* `tests\\test_gui_quick.py` 70 项（新增：模板列表/搜索过滤、双击模板写格子、\n  选中回填、批量、体检修复、机器码面板四个按钮）\n* 其余组不变，一键全绿：`python tools\\run_tests.py`\n\n## v0.4 — 2026-09-12\n\n**主题：把"玩法数据"也做出来（背包 / 召唤兽 / 经验）＋ 把游戏的反作弊整明白了 ＋ 能打包成 exe。**\n\n### 🎒 新的可改内容（都是实测数据结构）\n\n| 页签 | 能改什么 |\n| --- | --- |\n| 概览 / 快捷修改 | **存银**（游戏里就叫这个名，就是金钱）＋ 步数 / 存档次数 / 战斗次数 |\n| 背包 / 物品 | **4 页 × 20 格**：看、改数量、清空、**添加物品**（道具 / 武器 / 防具分开） |\n| 角色 / 属性 | 加了**获得经验**（`@exp`）与**升级所需经验**（`@limit_exp`） |\n| 召唤兽 | 等级·气血·魔法·TP·经验·**六项资质**·**忠诚**·寿命·成长·五维/潜能 + 常用预设 |\n| 开关 / 变量 | 补上游戏自己的名字（`MAP_SCROLL` / `PLOTING` / `CHOICS_COLUMN` / `DIALOGUE`） |\n\n背包为什么能"加"东西：游戏的容器是 `{槽号 => [RPG::Item 对象, 数量]}`，\n**物件本体就存在存档里**（不是只存 id），所以加物品要从 `Data\\Items.rvdata2`\n复制一份模板，再补上游戏自己加的 5 个字段\n（`@result_note` / `@attr` / `@update` / `@new` / `@transaction_code`）。\n\n### 🛡 反作弊：这次是真的搞清楚了（三重）\n\n1. **`Lock` 校验和**（v0.2 已解决）：金钱 `@master = @value*91+45+种子/800`。\n2. **周期检查**（v0.4 新发现）：脚本 29455 行起，**每 300 帧**查一次\n   ```\n   $jiance = [MAX_LEVEL_ACTOR*761205, MAX_LEVEL_BABY*761205,\n              MAX_GOLD*654321,     MAX_WAREHOUSE[1]*159753]\n   角色等级 > 60 / 出战召唤兽等级 > 65 / 存银 > 30,000,000 /\n   仓库页号 > 3 / 五维总点数 > 等级*10+500      → $game_system.cheated = 帧号\n   ```\n   一旦被标记：游戏内 20 分钟后弹警告、**25 分钟后 `msgbox "存档异常！<硬盘码>"` 并 exit**。\n   ⇒ 新增「防作弊体检」页签：列出每一项的当前值/上限、**一键按规则修复**\n   （把超限的压回上限）、**清除作弊标记**（`@cheated = false` + 去掉 keyword 里的 `VNE`）。\n   ⇒ 界面里的预设也相应改成合法上限（"满级(60)"、"召唤兽满级(65)"）。\n   ⚠ 顺便发现**当前这份存档 `@cheated` 已经是 134700**（说明之前就被判过作弊）——\n   工具里点一下「清除作弊标记」就干净了。\n3. **物品计数校验**（v0.4 新发现）：`$game_system.security[:items][id]` 是 `Change` 对象，\n   把"这件物品累计获得过几个"**逐位数字 AES-ECB 加密**存着（脚本 322 行 +\n   `AES_ECB.set_key(\'admin_1941344749\')`，1142 行）。\n   改背包数量后如果不同步，游戏下次**合法获得**同一件物品时会发现对不上 → 记作弊。\n   ⇒ `src/xj_aes.py` 用纯 Python 复刻了它的 AES-128-ECB（PKCS#7 填充、按位加密），\n   **改数量/加物品时自动把计数一起改对**；缺条目的按游戏模板补一个 `Change`；\n   另外提供「同步物品计数校验」按钮全量对齐。\n   验证方式很硬：拿存档里 7 个真实 `Change` 的密文反解，位数字全对得上。\n\n### 🧩 能"加减对象"了：整档重写（v0.4 的地基）\n\n以前只能做**等宽标量补丁**，因为 Ruby Marshal 的 `@N` 对象链接一旦成员数变了就会整体错位。\n这次把序列化器补齐，可以**整条顶层对象按 Ruby 的规则重新编号**：\n\n* `xj_marshal.serialize_doc(objects)`：每个顶层对象各带 `04 08` 头、各自独立的\n  **对象表 + 符号表**；\n* 符号要复刻 Ruby 的写法，否则**静默坏档**：定义时非 ASCII 才带\n  `I :@体质 <1 ivar> :E T` 包装，**链接（`;N`）不带包装**；\n* `tools/test_roundtrip.py`：17 个游戏明文（含 `save.rvdata2`、`AutoSave\\save00`，\n  300 KB+）**逐字节还原** ✅ —— 什么都不改时输出与原文完全一致，\n  所以整档重写不会引入任何意外差异。\n\n### 📦 打包成 exe\n\n`tools/build.py`（照 huaji1 的路子）：\n\n* `csc /platform:x86` 编自带的 32 位宿主 `XJCodec32.exe`；\n* `dist/` 里：`画迹2存档工具v0.4.exe` + **`XJCodec32.exe`**（必须挨着 exe；\n  `--dll-dir` 可以改成放 `dll/` 子目录，`xj_codec` 会按 目录 → dll/ → bin/ 找）\n  + `使用说明.txt` / `README.md`；\n* **不放**游戏的 `System\\main.dll`（版权 + 运行期就从用户自己的游戏目录加载）；\n* tcl/tk 脚本库要 `--add-data` 带上，并在 `import tkinter` 之前设 `TCL_LIBRARY`；\n* 打包后自带自检：`XJ_SELFTEST=1` 或 `--selftest` → 写 `selftest_result.txt`\n  （依赖查找、明文 MD5、背包/召唤兽/防作弊体检、整档重写自检）；\n* 崩溃会写 `error.log`（`--windowed` 没控制台）。\n\n### ✅ 测试（13 组 / 370+ 项，全绿）\n\n| 测试 | 项数 | 说明 |\n| --- | --- | --- |\n| `tests\\test_marshal.py` | 27 | |\n| `tests\\test_codec.py` | 15 | |\n| `tests\\test_model.py` | 12 | |\n| `tests\\test_gui.py` | 24 | 9 个页签都建得起来 |\n| `tests\\test_gui_quick.py` | 57 | 含背包改数量/加物品/清空、召唤兽、防作弊体检 |\n| `tools\\test_db_csv.py` | 56 | |\n| **`tools\\test_game_layer.py`** | 35 | **新**：背包 / 经验 / 召唤兽 / 计数校验同步 |\n| **`tools\\test_roundtrip.py`** | 18 | **新**：逐字节还原（`tools\\_plain\\*.bin` 全过） |\n| **`tools\\test_semantic_equal.py`** | 39 | **新**：整档重写的语义等价（含对象共享关系） |\n| `tools\\smoke.py` | 12 | |\n| `tools\\test_save_layer.py` | 14 | |\n| `tools\\verify_all.py` | 17 | |\n| **`tools\\test_dist.py`** | — | **新**：把 `dist\\` 拷到临时目录跑 exe 自检 |\n\n一键跑：`python tools\\run_tests.py`\n\n### 🧰 逆向用的新工具\n\n`dump_scripts.py`（游戏脚本全解成一整份 Ruby）、`grep_scripts.py`（中文关键词走\n`tools/_keys.txt`、结果直接写 UTF-8 文件，绕开 GBK 终端）、`probe_v04_*.py`\n（背包/召唤兽/属性结构）、`probe_security.py`（`Change` 密文）、`probe_attrs.py`。\n\n---\n\n## v0.3 — 2026-09-13\n\n**主题：界面照画迹1 重做，外加「Data → CSV」查表功能。**\n\n### 🎨 界面（重写 `src/xj_viewer.py`）\n\n照 `huaji1-save-editor` 的布局重排，8 个页签：\n\n1. **概览 / 快捷修改** —— 金钱 / 步数 / 存档次数 / 战斗次数，一键应用\n   + 「检查并修复防作弊校验」，旁边常显校验状态\n2. **全部解析数据** —— 左侧懒加载树（3 列：字段 / 值 / 说明），右侧节点详情，\n   右键「改值 / 复制 / 复制路径」，顶部**全局搜索**结果可双击跳转\n3. **角色 / 属性** —— 角色列表 + 基础字段 + `Game_Actor_Attr` 的**中文五维**\n   （体质/法力/力量/耐力/敏捷/潜能/人气/贡献/体力/活力），\n   快捷按钮：满级(99) / 回满 HP·MP / 属性全 +10\n4. **队伍 / 物品** —— 队伍成员 + 物品栏（名字取自 `Data\\Items.rvdata2`）\n5. **开关 / 变量** —— 双击切换 / 修改\n6. **数据表 (CSV)** —— 10 张 Data 表的预览 + 导出 CSV（本轮新增）\n7. **说明 / 机制** —— 密钥、存档结构、防作弊原理、CSV 使用方法\n8. **更新日志** —— 直接读 `CHANGELOG.md`\n\n其它：窗口 1220×800、`Ctrl+S` 保存、标题栏 `*` 表示有未保存修改、\n作者/开源地址/许可证状态栏、上次打开的存档自动记住\n（`~/.huaji2_save_editor_last.txt`）。\n\n### 📊 新增 `src/xj_db.py`：Data\\*.rvdata2 → CSV\n\n`Data\\*.rvdata2` **全都是加密的**（和存档同一套 main.dll，密钥 `761205`），\n现在自动解密 + 解析 + 转 CSV（`utf-8-sig`，Excel 双击直接正常显示中文）：\n\n| 默认导出 | 行数 | 可选导出 | 行数 |\n| --- | --- | --- | --- |\n| Items 物品 | 300 | Troops 敌人队伍 | 310 |\n| Weapons 武器 | 400 | CommonEvents 公共事件 | 100 |\n| Armors 防具 | 300 | | |\n| Skills 技能 | 320 | | |\n| States 状态 | 120 | | |\n| Actors 角色 | 300 | | |\n| Classes 职业 | 300 | | |\n| Enemies 敌人 | 700 | | |\n\n嵌套字段会翻成人话：\n\n* 物品 `@effects` → `HP回复 +500%；解除状态#1 0%`\n* 伤害 → `伤害:无 公式:0 浮动:20% 会心:否 属性:0`\n* `@features` → `最大HP+50%；状态有效度#1…`\n* 敌人 `@params` → `80/0/60/1/15/1/3/1`（列头已按"最大HP/MP/攻/防/魔攻/魔防/敏/运"排好）\n* 敌人 `@drop_items` → `物品#1（掉率 1/100）`，没配的空槽不显示\n* 职业 `@learnings` → `Lv1→技能#9`\n* 使用场合 / 影响范围 / 命中类型 / 类别 / 行动限制 / 触发方式等枚举也翻了中文\n\n命令行：\n\n```bat\npython src\\xj_db.py                       :: 列出所有表 + 行数\npython src\\xj_db.py --out csv             :: 默认 8 张表全转\npython src\\xj_db.py --all --out csv       :: 连 Troops/CommonEvents 一起\npython src\\xj_db.py Items Skills --out csv\n```\n\n> 查"某个物品 id 是什么 / 有什么效果"直接开 `csv\\Items_物品.csv` 就够用了，\n> 所以 `Map` / `System` / `Scripts` / `Tilesets` / `Animations` 不做转换。\n\n### 🐞 修复\n\n* 数据树改成**懒加载**（每层最多 300 项，展开才生成）：\n  之前打开真存档会 materialize 两万多个容器，界面直接卡死（用 `faulthandler`\n  抓到栈顶是 `_fill`，现在按需加载 + 模型层搜索 + 路径展开跳转）\n* 非本作存档（比如 `Logs\\Battle\\*\\Battle.bt2`）不再假装成功：\n  `SaveDoc` 是懒解析的，现在会校验必需分区，不是存档就只留「数据树」页可用\n* 载入 / 保存后的**面板刷新逐步兜底**：某一个面板出错不再把数据树一起带下去，\n  也不会再弹一个藏在窗口后面的模态框把界面挂住\n* 合并进来的一次修复：改角色 `@name` 报 `\'StrNode\' object has no attribute \'value\'`\n  （`PatchEngine.set_scalar` 现在按节点类型赋值）\n\n### ✅ 测试（总计 9 组 / 218 项，全绿）\n\n| 测试 | 项数 |\n| --- | --- |\n| `tests\\test_marshal.py` | 27 |\n| `tests\\test_codec.py` | 15 |\n| `tests\\test_model.py` | 12 |\n| `tests\\test_gui.py`（重写：8 页签 + 懒加载 + 表预览） | 23 |\n| `tests\\test_gui_quick.py`（重写：42 项，含改钱/角色/开关变量/修校验/存盘/CSV） | 42 |\n| `tools\\test_db_csv.py`（新增：10 张表解析 + CSV 编码/行数/中文表头） | 56 |\n| `tools\\smoke.py` | 12 |\n| `tools\\test_save_layer.py` | 14 |\n| `tools\\verify_all.py` | 17 |\n\n一键跑：`python tools\\run_tests.py`\n\n---\n\n## v0.2 — 2026-09-13\n\n**主题：把保护机制彻底破掉，让编辑器真的能用。**\n\n### 🔑 逆向：三个密钥全部拿到\n\n* `761205` —— `Data\\*.rvdata2` 数据库、`System\\Game.md5`\n  （用 32 位宿主内置的批量试密钥模式，在 main.dll 解壳镜像的字符串表里爆破命中）\n* `imoutogadaisuki` —— `Data\\Scripts.rvdata2`\n  （在 `761205` 附近的常量区取子串 + 变形爆破命中）\n* `tiyan_version` —— 存档 `save.rvdata2` / `AutoSave\\*.rvdata2`\n  （解开脚本本体后，直接读 `Config::File::SAVE_FILE_PASSWORD`）\n\n关键认识：`decryption_file` 的第三个参数**就是密钥字符串**；\n密钥不对时 DLL 只写 0 字节空文件（不报错），这正是爆破的判定依据。\nv0.1 里"main.dll 有出厂密钥状态、解不开游戏文件"的结论是**错的**。\n\n### 🛠 新增\n\n* `src/xj_save.py` —— 存档语义层：\n  * `gold()` / `set_gold()`（自动重算 `Lock` 校验和）\n  * `check_locks()` / `repair_locks()` —— 防作弊校验检查与一键修复\n  * 角色 `@level/@hp/@mp/@name`、开关、变量、队伍成员、物品读写\n  * `shield_seed()` / `lock_master()` —— 复刻 `v*91+45+seed/800`\n* GUI 新增「快捷修改」页签（金钱 / 角色 / 开关变量 / 防作弊修复 / 保存）\n* 自动选密钥：`key_for()` 按文件名猜，猜不出就**依次试三个密钥**\n  （这样连 `save.rvdata2.bak.20260913-120000` 这种备份也能直接打开）\n* 工具链：`run_brute.py`（崩溃容忍 + 多进程并行爆破）、`genkeys.py`、`probe_near.py`、\n  `dump_scripts.py`、`parse_diag.py`、`hexdump.py`、`verify_all.py`、\n  `test_save_layer.py`、`decrypt_all.py`、`cleanup.py`\n* 文档：`docs/存档格式.md`（重写）、`docs/逆向过程.md`（重写）、\n  `docs/待解决问题.md`（重写）、`README.md`、`使用说明.txt`\n\n### 🐞 修复（Marshal 解析器）\n\n* 支持 `}`（带默认值的 Hash）、`C`（String/Array/Hash 子类）、\n  `c`/`m`（类 / 模块对象，**且它们要占对象编号**）\n* 支持 `I` 包装的**非 ASCII 符号**（本作实例变量名是中文：`@体质`、`@法力`…），\n  且 `I` 包装**不额外**占对象编号\n* 加严格校验开关（链接越界 / 类名 / 实例变量名），配合 `tools/parse_diag.py`\n  可以打印出错时的递归容器链，定位错位快很多\n\n修完后：17 个明文文件（含 3 个存档、560 KB 的 `Classes.rvdata2`）全部完整解析，\n解析终点与文件末尾逐字节对齐，重写自洽（`tools/verify_all.py`）。\n\n### ✅ 测试\n\n* 单测 61 项全绿：`test_marshal` 27 / `test_codec` 15 / `test_model` 12 / `test_gui` 7\n* 新增冒烟：`tools/smoke.py`（解密→解析→加密往返，14 项）\n* 新增语义层回归：`tools/test_save_layer.py`（改金钱/开关/变量/角色 + 防作弊校验修复，14 项）\n\n---\n\n## v0.1 — 2026-09-12\n\n* 建立仓库骨架与 git 历史；`src/` 拆分出 `xj_env` / `xj_codec` / `xj_marshal` /\n  `xj_edit` / `xj_model` / `xj_viewer`，附 32 位宿主 `xj_codec32.cs`\n* 完成 Ruby Marshal 4.8 解析 / 序列化 / 区间补丁；tkinter 界面（概览 / 数据树 / 说明）\n* 摸清 `main.dll`：MPRESS 加壳、导出表、参数必须 UTF-8、8 字节分组 ECB、\n  密文长度是 8 的倍数\n* 文档：`docs/存档格式.md`、`docs/逆向过程.md`、`docs/待解决问题.md`\n* **已知限制（v0.2 已解决）**：当时误判为"main.dll 出厂密钥状态与游戏不一致"，\n  因此解不开游戏自己的密文文件\n'
