# 待解决的问题

> v0.2 状态：**保护机制已经全部解决**，存档可以正常解密 / 解析 / 修改 / 写回。
> 下面只剩一些"能做但还没做"的增强项。

## 已完成（不用再查了）

- [x] 找到 `System\main.dll` 三个密钥：`761205` / `imoutogadaisuki` / `tiyan_version`
- [x] 解开 `Data\Scripts.rvdata2`，拿到游戏全部 Ruby 源码
- [x] Ruby Marshal 4.8 解析器能完整吃下 17 个明文文件（含 400+ KB 的 Classes 与 3 个存档）
- [x] 存档 `header + contents` 两段式结构、11 个分区全部识别
- [x] 防作弊 `Lock` 校验（`v*91+45+seed/800`）复刻并自动修复

## 待增强

1. **物品 / 装备 / 技能的图形化增删**
   现在 `xj_edit` 只做了"标量就地改 + 子树自包含重写"。
   往 `@items` / `@weapons` / `@armors` 里加新条目需要往 Hash 里插一对
   （会改变成员数 → 必须整块重写并展开 `@N` 链接）；
   `xj_edit.serialize_tree` 已经支持自包含序列化，只差把它接到语义层上。
   注意 `@items` 的值是 `[数量, …]` 数组，而物品本体在 `Data\Items.rvdata2` 里
   （游戏用 id 引用），所以"加物品"只需要在 Hash 里加 `id => [数量, 0]`。

2. **角色全套属性**（`@skills` / `@equips` / `@exp` 是 Hash / `Game_Actor_Attr` 的中文属性）
   `:actors` → `Game_Actors.@data[id]` → `Game_Actor`；属性实际存在
   `Game_Actor_Attr`（`@体质/@法力/@力量/@耐力/@敏捷/@潜能/@人气/@贡献/@体力/@活力`）。
   这几个字段名是中文，正好也是"非 ASCII 符号被包 `I`"那个坑的来源 —— 解析侧已经解决。

3. **删掉 `I` 包装时的编码校验**
   现在写回字符串时不会附带 `:E => true` 的编码 ivar。对 ASCII 名字没影响，
   但如果以后要改**名字**为中文，游戏可能按"ASCII 字符串"读——需要时再补。

4. **GUI 还没做的**：搜索结果的"上一个/下一个"、批量改数组、
   "把 N 个物品数量一键×10"之类的便捷操作。

5. **打包成 exe**（PyInstaller）并带上一份 `XJCodec32.exe`，方便不装 Python 的玩家。

## 已知限制（不是 bug）

- 游戏运行中改存档无效：它只在存/读档时读文件。
- 改了存档内容后**不要**再去改 `Data\main.rvdata2` / `main.dll`：
  `System\Game.md5` 里存着它们的 md5。（改存档/Data 是不校验的。）
- 存档里的 `@config[:hard_disk_code]` 是本机硬盘码，
  把存档拷到别的机器上可能被判定异常 —— 换机器玩请用游戏自己的存档。
