# 待解决的问题

> v0.4.1 状态：**保护机制 + 玩法数据全部打通**；界面 9 个页签；
> 已能打包成 exe（`tools\build.py`），宿主静默不弹黑框；
> 背包照画迹1 补齐（模板搜索/写入/体检修复）；机器码可读可写。
> 下面只剩一些"能做但还没做"的增强项。

## 已完成（不用再查了）

- [x] 找到 `System\main.dll` 三个密钥：`761205` / `imoutogadaisuki` / `tiyan_version`
- [x] 解开 `Data\Scripts.rvdata2`，拿到游戏全部 Ruby 源码
- [x] Ruby Marshal 4.8 解析器能完整吃下 17 个明文文件（含 400+ KB 的 Classes 与 3 个存档）
- [x] **整档字节级重写**：`xj_marshal.serialize_doc()` 重写后与原明文**逐字节相同**（17/17）
- [x] 存档 `header + contents` 两段式结构、11 个分区全部识别
- [x] 防作弊 `Lock` 校验（`v*91+45+seed/800`）复刻并自动修复
- [x] **存银**（游戏里就叫这个名）改名 + 同步 `@master` ✔
- [x] **背包 4 页×20 格**：改数量 / 清空 / 添加物品（自动同步计数校验）✔
- [x] **物品模板列表 + 搜索 + 写进选中格子 / 放进空格子**（照画迹1）✔
- [x] **背包体检 + 一键修复**（重复格合并、超上限截断、坏格清空）✔
- [x] **机器码（存档绑定）**：读本机 `get_hard_disk_character`、看存档记录、
      一键追加/替换（换机器玩）✔
- [x] **物品计数校验 `Change`**：AES-128-ECB（`admin_1941344749`）读写 + 按实际背包重算 ✔
- [x] 角色中文五维（`Game_Actor_Attr`）读写 + **获得经验 `@exp`**（可突破等级上限）✔
- [x] **召唤兽**（`Game_Baby` / `Game_Baby_Attr`）：等级/气血/魔法/TP/经验/六项资质/
      忠诚/寿命/成长/五维 + 一键预设 ✔
- [x] **防作弊体检**：按游戏自己的规则逐项检查 + 一键修复 + 清除 `@cheated` ✔
- [x] 数据树懒加载 + 全局搜索 + 路径跳转；**字段名不再出现 `[None]`**，全部带中文注释 ✔
- [x] `Data` 10 张表解密解析 + 转 CSV（含嵌套字段翻人话）✔
- [x] **打包 exe**：`tools\build.py`（自带 32 位宿主 + 自检）✔

## 待增强

1. **技能 / 装备的可视化编辑**
   `@skills`（id 数组）、`@equips`（6 个 `Game_BaseItem`）现在只能看（角色页只读），
   改要进数据树。

2. **写回字符串时补 `:E => true` 编码 ivar**
   现在写字符串不附带编码 ivar。对 ASCII 名字没影响，
   但如果以后要把**名字改成中文**，游戏可能按"ASCII 字符串"读——需要时再补。
   （链接与符号定义的写法规则见 `docs\开发指南.md` §5。）

3. **GUI 还没做的**：搜索结果的"上一个/下一个"、批量改数组、
   "把 N 个物品数量一键×10"之类的便捷操作；
   背包已能批量设数量/模板写入，但还不能多选几格一起改。

4. **CSV 的进阶用法**：把 `@effects` 里的 id 换成对应名字
   （比如 `解除状态#1` → `解除状态#1 中毒`），需要跨表引用
   （`Item.@effects` → `States.rvdata2`）。

5. **召唤兽技能 / 内丹**：现在只读了资质与五维，
   `Game_Baby` 里的技能列表还没做图形化。

## 已知限制（不是 bug）

- 游戏运行中改存档无效：它只在存/读档时读文件。
- 改了存档内容后**不要**再去改 `Data\main.rvdata2` / `main.dll`：
  `System\Game.md5` 里存着它们的 md5。（改存档/Data 是不校验的。）
- 存档里的 `@config[:hard_disk_code]` 是本机硬盘码，
  把存档拷到别的机器上可能被判定异常 —— 换机器玩请用游戏自己的存档。
- `@cheated`（作弊标记）是个**帧计数定时炸弹**：不清的话，
  游戏中连续玩到“帧数 − @cheated > 89877”（约 62 分钟）时会弹「存档异常」并退出。
  工具里点「清除作弊标记」即可归零。
