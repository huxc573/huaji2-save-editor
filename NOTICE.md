# 授权范围与例外

本仓库的 **MIT 协议只覆盖仓库里自己写的代码与文档**（`src/xj_*.py`、
`src/xj_codec32.cs`、`tools/`、`tests/`、`probes/`、`docs/`、`README.md` 等）。

## 不包含、也不分发的东西

| 东西 | 说明 |
|---|---|
| 游戏本体 | 《画迹2：缘起凡尘》的 `Game.exe`、`Data\*`、`Audio\*`、`Graphics\*` 等全部素材，版权归游戏原作者 |
| `System\main.dll` | 游戏的加密/防作弊库（MPRESS 加壳）。**本仓库不含、也不分发它**；`XJCodec32.exe` 在运行时从**用户自己机器上的游戏目录**加载 |
| 游戏脚本 | `Data\main.rvdata2`、`Data\Scripts.rvdata2` 等，版权归游戏原作者 |
| 反编译/反汇编产物 | `probes/` 里的探针只**生成到本地临时文件**（已被 `.gitignore` 排除），仓库里不放游戏的任何数据 |

## 关于 `probes/`

`probes/` 下是**逆向过程的可复现脚本**，用途是记录"结论是怎么得到的"，
便于后续版本继续研究。它们：

* 只读用户本机的游戏文件，不修改游戏本体；
* 不包含游戏的任何字节内容；
* 不包含 `main.dll` 的拷贝或解壳后镜像（那是运行期在本地生成的临时文件）。

## 关于 `XJCodec32.exe`

`src/XJCodec32.exe` 由本仓库的 `src/xj_codec32.cs` 用系统自带的
`csc.exe` 编译而成（`tools/build_host.py` 可复现）。
它**不含任何第三方代码**，只在运行时把 `main.dll` 里的加解密函数借出来用。

## 使用者的责任

* 只在**自己拥有正版**的游戏上使用；
* 修改存档前自行备份；
* 不要把本工具用于在线排行、多人对战等会破坏公平性的场景；
* 遵守游戏原作者的使用条款。
